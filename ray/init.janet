# ~/.config/ray/init.janet — personal ray preferences, evaluated on boot after
# the bundled prelude/live scripts (so the full API is available). Deployed to
# every machine via home-manager (home/common.nix). Optional & non-fatal.

# Dark wireframe editor, matching Doom Emacs (doom-one).
(set-wireframe-theme :dark)
