# Memory Index

- [Embedded lang benchmarks](project_embedded_lang_benchmarks.md) — 6-language Rust embedding benchmark suite; v4 report complete; ECL leads shader-math (0.09 µs), Janet 2nd, Steel best pure-Rust; LuaJIT disqualified (no ARM64 JIT); all ray POCs at Stage 4
- [ray-ecl-integration](project_ray_ecl_integration.md) — ECL push-arch scripting for ray; REVERTED off ray main 2026-07-25 → now ONLY on ecl-scripting branch (origin/ecl-scripting @2045750, ray main back at 9888b98); verified still builds (`--features ecl`, `nix develop .#ecl`, ECL 26.5.5); Slynk 4005; Darwin -liconv + edition-2024 bindgen gotchas solved
- [ray-ecl-poc](project_ray_ecl_poc.md) — standalone ECL POC (pull model, precursor to ray-ecl-integration); Slynk (port 4005); Stage 1+2+3+4 done; 2.6× Lambert (Linux), 1.1× (macOS ARM64); tex=0.18 µs Linux / 0.05 µs macOS; SIGFPE footgun, Nix dev shell required
- [ray-steel-poc](project_ray_steel_poc.md) — Steel (Scheme) POC; MIGRATED 2026-07-25 to PUSH architecture (ECL/Janet parity): Rust register_fn %-callbacks, Steel calls them; scripted shaders removed; 3-file prelude/scene/live layout; parity 0 diffs; TCP 4006 + steel> prompt + CAPF/Corfu completion via (%symbols) sentinel; pure cargo build; void is a value not a proc, f64 rejects IntV
- [ray-janet-poc](project_ray_janet_poc.md) — Janet 1.41 POC; MIGRATED 2026-07-25 to PUSH architecture (full ECL parity): Rust registers %-cfunctions, Janet calls them; scripted shaders removed; 3-file prelude/scene/live layout; parity 0 diffs; TCP 4007 + comint + CAPF kept; Nix dev shell required
- [ray POC collaboration feedback](feedback_ray_poc_collaboration.md) — stage-by-stage confirmation, honest assessments wanted, one-word approval means implement directly
- [Autonomous implementation](feedback_autonomous_implementation.md) — after a "go" on approved multi-step work, run to completion autonomously (no per-stage approval), report at end; stage-gating was for POC exploration only
- [ray next steps](project_ray_next_steps.md) — **DECISION 2026-07-26: Janet chosen over ECL** for ray scripting (non-commercial/creative work → safety-by-construction > perf; ECL SIGFPE trap + informal Slynk thread-race were dealbreakers). Migration Phase 1 + 2 DONE and **MERGED TO MAIN 2026-07-27** (fast-forward, origin/main @665cbed): feature-gated Janet in ray proper; flag-driven CLI (--scene optional/--out/--tcp opt-in, --window deferred, dropped --headless/--parity); janet> terminal prompt; no default scene (bare REPL, load-scene); friendlier --scene errors; janet-connect auto-opens the --scene buffer via *scene-file*. Next: --window design decision (if wanted), then new render features. Also done: Janet doc-popups, janet-lsp coexistence
- [ray-janet render params](project_ray_janet_render_params.md) — deferred design: keep (render) nullary, move render params into a Janet table `*render* @{:path ...}`; string-read now UNBLOCKED (scripting::eval_string exists); concrete deferred item (2026-07-28): make saved-PNG annotation optional (clean vs stamped) — smallest = `:stamp` flag + `*save-annotated*` var, tidy = `:annotate` in the `*render*` table
- [ray-janet scene architecture](project_ray_janet_scene_architecture.md) — READ-LATER design note: scene-as-Janet-data (immediate mode) vs retained live scene in Rust; kept current model (single source of truth, parity, cross-lang API); retained mode's real win = incremental/refit BVH; hybrid = dirty-flag incremental push
- [Repo paths](reference_repo_paths.md) — local paths for nixos, ray, ray-janet-poc, ray-steel-poc, ray-ecl-poc; SSH key ~/.ssh/id_ed25519 for rust repos (nixos itself is HTTPS); passphrase auto-handled — macOS Keychain, Linux ssh-agent (once/boot)
- [Doom config nix rebuild](reference_doom_config_nix_rebuild.md) — ~/.config/doom/*.el are home-manager nix-store symlinks; edits to ~/nixos/doom/ need `darwin-rebuild switch --flake ~/nixos#kevmac` to reach Emacs (else silently stale); files loaded by absolute live path (e.g. ~/devel/ray/emacs/janet-live-mode.el) need only an Emacs restart
- [Janet docstring position](reference_janet_docstring_position.md) — Janet defn docstring only registers as :doc when BEFORE the arg list, not after (silent no-op otherwise); bit the ray-janet-poc completion doc popups
- [Janet Emacs signature help](reference_janet_emacs_signature_help.md) — eglot's automatic dim signature line vs the TAB completion doc box; both KEPT; drop commands noted for future disable
- [Doom janet-lsp routing](reference_doom_janet_lsp_routing.md) — user's Doom surfaces LSP diagnostics via flycheck-eglot NOT flymake (so flymake-mode/eglot-stay-out-of are no-ops); janet-lsp reaches janet only via ray-janet-coexist; ray-studio now defaults janet-lsp OFF; stale POC janet-live-mode load path in ~/nixos/doom/config.el
- [ray-studio LSP future options](project_ray_studio_lsp_future.md) — DEFERRED 2026-07-27: workbench runs janet-lsp OFF (live-REPL CAPF only); user trialing it before deciding; future options = live-connection eldoc, scripts-scoped goto-def, janet-lsp stub-file, or just re-enable & tolerate false positives
